# Local verification record

Generated by `python scripts/local_verification.py` using FastAPI's in-process test client.
The optional provider variables are cleared by the script, so this record does not call a
remote service or measure provider behavior.

| Check | Observation |
| --- | --- |
| `/health` | HTTP 200; `2` indexed chunks |
| `/ready` | HTTP 200; `ready=True` |
| Supported `/ask` | HTTP 200; mode `evidence-only`; fallback `False` |
| Evidence contract | `retrieved-markdown-chunk-v1`; `1` returned item; citation `[capstone_incident_response.md#0]` |
| Source metadata | `capstone_incident_response.md` chunk `0`; `700`-character snippet |
| Unknown `/ask` | HTTP 200; `0` evidence items |
| Extra request field | HTTP 422; rejected |
| Frontend | HTTP 200; DOM text APIs `True`; unsafe HTML API list empty |
| Provider calls | `0` |

This is a local behavior record for the checked source tree. It is not a retrieval-quality
benchmark, latency baseline, uptime result, monitoring result, or deployment evidence.
